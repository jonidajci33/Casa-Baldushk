<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="profisc.Ord"
       t-inherit="point_of_sale.ActionpadWidget"
       t-inherit-mode="extension">

<!--        &lt;!&ndash; Hide the original Order button &ndash;&gt;-->
<!--        <xpath expr="//button[hasclass('submit-order')]" position="attributes">-->
<!--            <attribute name="t-if">false</attribute>-->
<!--        </xpath>-->

      <!-- Replace the original Order button with Order to Kitchen button -->
        <xpath expr="//button[hasclass('submit-order')]" position="replace">
            <button
                class="submit-order h-100 button btn btn-lg d-flex align-items-center w-50 flex-fill position-relative px-3"
                t-att-class="swapButtonClasses"
                t-on-click="onClickSendOrder"
            >Order to Kitchen
            </button>
        </xpath>
    </t>
</templates>